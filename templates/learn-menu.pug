nav.nav-docs
  a.logo.logo-ampersand(href="/")
  a.section-top(href= '/learn/', class= indexActive ? 'active' : '') Guides

  for page in pages
    a.section-sub(href= '/learn/' + page.url, class=(page.url === currentPageUrl ? 'active' : ''))
      = page.meta.pagetitle
